{
  "name": "react-moving-slider",
  "version": "0.1.1",
  "description": "A simple React component for a different slider visualization",
  "main": "MovingSlider.js",
  "private": false,
  "dependencies": {
    "react": "^17.0.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "publish:npm": "rm -rf public && mkdir public &&  babel src/Components -d ./ --copy-files",
    "eject": "react-scripts eject"
  },
  "babel": {
    "presets": [
      "@babel/preset-react"
    ]
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },

  "devDependencies": {
    "@babel/cli": "^7.15.4",
    "@babel/preset-react": "^7.14.5"
  },
  "keywords": [
    "react",
    "moving-slider",
    "slider",
    "dynamic"
  ],
  "author": "Michelangelo Alesi",
  "license": "ISC"
}
